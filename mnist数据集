1、MNIST数据集中的图像是28*28个像素，将这个二维数组展开成一个一维向量，长度是28*28=784，共包含60000张图片，所以训练的数据的为[60000,784]
第一个维度用来索引图片，第二个维度索引每张图片的像素点。图片中每个像素的像素值强度介于0-1之间。 

2、标签是0-9，转化为“one-hot vectors”，一个one hot除了某一位数是1以外其余都是0,0标签([1,0,0,0,0,0,0,0,0])，则label是[60000,10]

3、输入784（一张图的一个像素点作为输入），输出10（10个标签）

4、softmax模型用来给不同的对象分配概率
